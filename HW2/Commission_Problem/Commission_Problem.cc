#include "Commission_Problem.h"
#include <cstdio>
#include <cstring>

using namespace std;

double Solve_Commission_Problem(int lock, int stock, int barrel) {
/*
    double sales;
    double commission;
    if (lock == -1)
        return 0;
    else if ((lock < 1 || lock > 70) || (stock < 1 || stock > 80) || (barrel < 1 || barrel > 90))
        return -1;
    sales = lock * 45 + stock * 30 + barrel * 25;
    if (sales < 1000)
        commission = sales * 0.1;
    else if (sales < 1800) {
        commission = 1000 * 0.1;
        commission += (sales - 1000) * 0.15;
    }
    else {
        commission = 1000 * 0.1;
        commission += 800 * 0.15;
        commission += (sales - 1800) * 0.2;
    }
    return commission;
*/	
	return -999;
}

